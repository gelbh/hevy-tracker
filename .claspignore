# Ignore everything by default
**/**

# Allow Apps Script files (patterns are relative to rootDir)
# Modern clasp supports .gs files natively, no conversion needed
!appsscript.json
!**/*.gs
!**/*.html

# Explicitly ignore certain files/folders
.git/**
node_modules/**
